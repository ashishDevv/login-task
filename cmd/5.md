I am building a production-grade Go backend system and I need deep, practical guidance on observability design using OpenTelemetry, Zerolog, and related tooling.

Here is my full context:

🧠 Architecture Overview

I follow a clean architecture pattern:

- main acts as orchestrator (composition root)
- Infra layer initializes external systems (Postgres, Redis, etc.)
- Dependency container wires repositories, services, handlers
- Handlers are the only layer exposed to routers
- Everything returns errors (no logging or printing except at boundaries)
- Logger (Zerolog) is injected as a dependency
- context.Context is passed everywhere

🧱 Stack

- Language: Go
- Logging: Zerolog
- Tracing & Metrics: OpenTelemetry
- HTTP instrumentation: otelhttp
- DB: PostgreSQL using pgx (with otelpgx)
- Redis: go-redis with redisotel
- Metrics backend: Prometheus
- Tracing backend: Jaeger
- Visualization: Grafana

🔍 What I Have Implemented

Logging

- Structured logging with Zerolog
- Logger injected into services/repositories
- Request-scoped logger stored in context via middleware
- trace_id added to logs for correlation
- Avoid logging in constructors and infra

Tracing

- otelhttp middleware for HTTP tracing
- Manual spans in service layer
- DB tracing via otelpgx
- Redis tracing via redisotel
- HTTP client tracing via otelhttp transport
- Sampling enabled (e.g., 10%)

Metrics

- Request count
- Request latency histogram
- Labels: method, route, status
- Prometheus scraping
- Grafana dashboards

Error Handling in Tracing

- Using span.RecordError(err)
- Using span.SetStatus(codes.Error, ...)
- Distinguishing between:
  - real errors (DB failure, infra failure)
  - expected errors (validation, 400, 404)

---

🚀 Advanced Scenario (IMPORTANT)

I am building a Redis-based job scheduler system:

Flow:

User submits job
→ job stored in DB + Redis sorted set (by scheduled time)
→ scheduler runs infinite loop:
- polls Redis sorted set
- fetches next N jobs
- pushes jobs into Go channel
→ worker pool (100 workers):
- consumes from channel
- executes jobs

This is an async system, not request-response.

---

🎯 What I Need Help With

I want deep, practical, senior-level guidance on:

1. Tracing Design (Async System)

- How to model traces across async boundaries
- How to propagate trace context via Redis / job payload
- Where to start and continue spans:
  - job submission
  - scheduler polling
  - enqueueing
  - worker execution
- How to design span hierarchy for this pipeline

---

2. Metrics Design for Scheduler

What metrics should I track, such as:

- queue size
- scheduling delay (very important)
- job execution latency
- throughput (jobs/sec)
- worker utilization

Also:

- how to design labels properly
- how to avoid high-cardinality issues

---

3. Logging Strategy

- Where to log in async pipeline
- What to log (job lifecycle events)
- How to avoid noisy logs in infinite loops

---

4. Span Design (Advanced)

I want deep understanding of:

- span.SetAttributes()
- span.AddEvent()
- when to use each
- real-world usage patterns

---

5. Span Hierarchy Design

- how to design clean span trees
- how many spans are too many
- what should and should not be a span

---

6. Performance Overhead

Given my stack:

- Zerolog
- OpenTelemetry tracing + metrics
- otelhttp, otelpgx, redisotel

I want:

- realistic overhead analysis
- what configurations matter most (sampling, logging volume, labels)
- best practices to keep overhead low

---

7. Best Practices & Tradeoffs

- When NOT to create spans
- When to skip DB tracing
- How to balance simplicity vs observability depth

---

⚠️ Expectations from you

- Don’t give generic explanations
- Focus on real-world, production-grade patterns
- Use concrete Go examples
- Explain reasoning (why, not just how)
- Highlight common mistakes
- Prefer clarity over jargon

---

🎯 Goal

I want to design an observability system where:

- Logs, traces, and metrics are correlated
- I can debug:
  - slow jobs
  - stuck jobs in queue
  - failing workers
- I can monitor:
  - system health
  - backlog
  - latency
- System remains performant and scalable

---

Now guide me step-by-step like a senior engineer reviewing this system.